import { ItemsFilterByCategoryPipe } from './items-filter-by-category.pipe';

describe('ItemsFilterByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsFilterByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
